/* ============================================================
   OLYMPIAN HEALTH SOLUTIONS — Client Portal config
   ------------------------------------------------------------
   Paste the two values from your Supabase project here.
   Find them in: Supabase Dashboard → Project Settings → API
     • Project URL      → SUPABASE_URL
     • anon / public key → SUPABASE_ANON_KEY
   The anon key is SAFE to ship in client-side code — your data
   is protected by Row-Level Security in the database, not by
   hiding this key. (Never put the *service_role* key here.)
   ============================================================ */
window.OHS_PORTAL = {
  SUPABASE_URL:      "https://ekeqkkkopancnpuztbsh.supabase.co",
  SUPABASE_ANON_KEY: "sb_publishable_V6uHhWhvfzPg3hpa-EWKJA_f1ECYYG_"
};
